{
  "name": "@notera/backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Notera-Health-Ai orchestrator: deid → NER → multi-agent pipeline → schema → guardrails → Firestore",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.9.0",
    "@google-cloud/speech": "^6.7.0",
    "ajv": "^8.17.1",
    "express": "^4.19.2",
    "google-auth-library": "^9.14.0",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=20"
  }
}
